enfrente	   (e)
derecha		   (d)
izquierda	   (i)
enfrente-derecha   (e-d)
enfrente-izquierda (e-i)
atras-derecha	   (a-d)
atras-izquierda	   (a-i)

  choque-frontal
  choque-hueco-angosto
  choca-oblicuamente-derecha-nada
  choca-oblicuamente-izquierda-nada
  choque-oblicuo-rodeado

let direccion-giro random 2

// Caso en el que el agente se encuentra de frente con un insecto
SI e Y !d Y !i Y !e-d Y !e-i ENTONCES
	SI heading > 45 Y heading < 135 ENTONCES
		SI heading > 45 Y heading < 90 ENTONCES
			heading = 0
		SINO
			heading = 180
	SI heading > 135 Y heading < 225 ENTONCES
		SI heading > 135 Y heading < 180 ENTONCES
			heading = 90
		SINO
			heading = 270
	SI heading > 225 Y heading < 315 ENTONCES
		SI heading > 225 Y heading < 270 ENTONCES
			heading = 180
		SINO
			heading = 0
	SI heading > 315 Y heading < 360 O heading >= 0 Y heading < 45 ENTONCES
		SI heading > 315 Y heading < 360 ENTONCES
			heading = 270
		SINO
			heading = 90

// Caso en el que el agente se encuentra de frente con un insecto, pero en un hueco angosto
SI e Y d Y i Y e-d Y e-i Y a-d Y a-i ENTONCES
	SI heading >= 45 Y heading < 135 ENTONCES
		heading = 270
	SI heading >= 135 Y heading < 225 ENTONCES
		heading = 0
	SI heading >= 225 Y heading < 315 ENTONCES
		heading = 90
	SI heading >= 315 Y heading < 360 O heading >= 0 Y heading < 45 ENTONCES
		heading = 180

SI e-i Y e-d Y !a-i Y !a-d ENTONCES
	SI heading >= 0 Y heading < 90 ENTONCES
		heading = 90
	SI heading >= 90 Y heading < 180 ENTONCES
		heading = 180
	SI heading >= 180 Y heading < 270 ENTONCES
		heading = 270
	SI heading >= 270 Y heading < 360 ENTONCES
		heading = 0

// Caso en el que el agente se encuentra de manera oblicua con un insecto, donde a su derecha no hay nada
SI e-i Y !e-d Y !d ENTONCES
	SI heading >= 0 Y heading < 90 ENTONCES
		heading = 90
	SI heading >= 90 Y heading < 180 ENTONCES
		heading = 180
	SI heading >= 180 Y heading < 270 ENTONCES
		heading = 270
	SI heading >= 270 Y heading < 360 ENTONCES
		heading = 0

// Caso en el que el agente se encuentra de manera oblicua con un insecto, donde a su izquierda no hay nada
SI e-d Y !e-i Y !i ENTONCES
	SI heading >= 0 Y heading < 90 ENTONCES
		heading = 90
	SI heading >= 90 Y heading < 180 ENTONCES
		heading = 180
	SI heading >= 180 Y heading < 270 ENTONCES
		heading = 270
	SI heading >= 270 Y heading < 360 ENTONCES
		heading = 0

// Caso en el que el agente se encuentra de manera oblicua con un insecto, en una esquina en la que el agente termina rodeado
SI e Y d Y i Y e-d Y e-i Y !a-d Y !a-i ENTONCES
	SI heading >= 0 Y heading < 90 ENTONCES
		SI heading >= 0 Y heading < 45 ENTONCES
			heading = 270
		SINO
			heading = 180
	SI heading >= 90 Y heading < 180 ENTONCES
		SI heading >= 90 Y heading < 135 ENTONCES
			heading = 0
		SINO
			heading = 270
	SI heading >= 180 Y heading < 270 ENTONCES
		SI heading >= 180 Y heading < 225 ENTONCES
			heading = 90
		SINO
			heading = 0
	SI heading >= 270 Y heading < 360 ENTONCES
		SI heading >= 270 Y heading < 315 ENTONCES
			heading = 180
		SINO
			heading = 90